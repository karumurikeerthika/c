#include <stdio.h>

int main() {
    int T, N;
    scanf("%d", &T);

    while(T--) {
        scanf("%d", &N);

        if(N < 2)
            printf("0\n");
        else
            printf("%d\n", (N - 2) / 7 + 1);
    }

    return 0;
}
