#include <stdio.h>

int main() {
    int T;
    long long A, B;

    scanf("%d", &T);

    while(T--) {
        scanf("%lld %lld", &A, &B);

        if(A > B)
            printf(">\n");
        else if(A < B)
            printf("<\n");
        else
            printf("=\n");
    }

    return 0;
}
