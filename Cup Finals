#include <stdio.h>
#include <stdlib.h>

int main() {
    int T;
    scanf("%d", &T);

    while(T--) {
        int X, Y, D;
        scanf("%d %d %d", &X, &Y, &D);

        if (abs(X - Y) <= D)
            printf("YES\n");
        else
            printf("NO\n");
    }

    return 0;
}
